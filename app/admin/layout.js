export const metadata = {
  title: "Art Studio Inspire — Owner dashboard",
  robots: { index:false, follow:false, nocache:true }
};

export default function AdminLayout({ children }) { return children; }
